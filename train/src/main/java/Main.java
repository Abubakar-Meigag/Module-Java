import module.Train;

import java.util.Arrays;

public class Main {

    public static void main(String[] args) {

        Train train = new Train();

        train.getStringElement(train.arr);
        train.getIntElement(train.arrayList);
        train.getNewArray(train.newArray);
        train.getOldArray(train.oldArray);
    }


}
